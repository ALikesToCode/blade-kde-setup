#!/usr/bin/env bash

# Native Wayland avoids the XWayland copy path and keeps fractional scaling,
# input, and high-refresh presentation aligned with Plasma.
set -Eeuo pipefail

export MOZ_ENABLE_WAYLAND=1
export MOZ_DBUS_REMOTE=1

exec /usr/bin/zen-browser "$@"
